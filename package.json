{
  "name": "execution-protocol",
  "version": "1.0.0",
  "description": "Execution Protocol v1.0 — Multi-tenant deterministic middleware for AI agent swarms. Policy enforcement, session isolation, verification-as-a-service.",
  "main": "server.mjs",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "test": "node --test tests/basepay*.test.mjs"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "ethers": "^6.13.5",
    "express": "^4.18.2",
    "express-rate-limit": "^8.2.1",
    "zod": "^3.22.4"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "ai-agents",
    "deterministic",
    "policy-enforcement",
    "multi-tenant",
    "verification"
  ]
}
